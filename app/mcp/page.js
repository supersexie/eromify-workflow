import MCPPage from "@/components/MCPPage";

export const metadata = {
  title: "MCP & CLI — Eromify",
  description: "Connect Eromify to Claude, Cursor, OpenClaw, and Hermes via MCP, CLI, or Skill.",
};

export default function Page() {
  return <MCPPage />;
}
