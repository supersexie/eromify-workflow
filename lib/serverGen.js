// Formerly wrote every finished gen to Vercel Blob generations.json.
// Disabled — web history lives in localStorage; MCP still uses addGeneration
// server-side. Keeping this stub so callers don't break.
export function recordServerGen() {}
